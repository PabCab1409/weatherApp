el fetchdata que hago en app.jsx podria llevarmelo a util tambien, y exportar la variable json, para que en vez de pasarsela a los componentes por props, la importe directamente en el componente donde tenga que usarla
Cuando hago el forecast, el array me devuelve la prevision de 6 dias, pero el primer dia del array, es el que uso para la caja grande. Buscar una solucion para que la prevision sea a partir del dia siguiente
Un rename a todas las clases no vendria mal crack
la informacion "compleja" (humedad y todo eso), viene en una medida inglesa, creo que habia una opcion en la pagina de obtener los datos en medida europea, mirar si puedo obtenerlos asi desde la api, y si no parsearlos.
